<div>
    <div>
        Title {{blogpost.title}}
    </div>
    <div>
        Text: {{blogpost.text}}
    </div>
    	<div>
		<button class="del-btn" data-id="{{blogpost.id}}">Delete</button>
	</div> 
    <div>
        <form class="form edit-form" data-id="{{blogpost.id}}">
            <div class="form-group">
                <label for="title-edit">New title:</label>
                <input class="form-input" type="text" id="title-edit" />
            </div>
            <div class="form-group">
                <label for="text-edit">New Text:</label>
                <input class="form-input" type="text" id="text-edit" />
            </div>
            <div class="form-group">
                <button class="btn btn-primary" type="submit">change!</button>
            </div>
        </form>
    </div>
</div>
<script src="/js/dashboard.js"></script>